# Deployment Guide - GitHub to Cloudflare Pages

## Автоматический деплой на Cloudflare Pages

Этот репозиторий настроен для автоматического деплоя статических HTML билдов на Cloudflare Pages через GitHub Actions.

## Настройка (первый раз)

### 1. Добавьте секреты в GitHub

Перейдите в **Settings** → **Secrets and variables** → **Actions** → **New repository secret**

Добавьте следующие секреты:

| Name | Value |
|------|-------|
| `CLOUDFLARE_API_TOKEN` | `5YA9j_EauFJP772qcvl_Rzsktgk2Bpr3oSiLjQkX` |
| `CLOUDFLARE_ACCOUNT_ID` | `925262faebbd07d9f3636c6a04c61b6d` |
| `CLOUDFLARE_PROJECT_NAME` | `burky-cz` |

### 2. Проверьте структуру репозитория

Убедитесь, что ваши билды находятся в:
```
wp-content/uploads/builds/
├── version-123/
│   ├── index.html
│   └── img/
├── version-124/
│   ├── index.html
│   └── img/
```

## Как деплоить

### Автоматический деплой (рекомендуется)

1. Сгенерируйте билд через WordPress админку
2. Закоммитьте изменения:
   ```bash
   git add wp-content/uploads/builds/
   git commit -m "Add new build version"
   git push origin main
   ```
3. GitHub Actions автоматически задеплоит **последний** билд на Cloudflare Pages
4. Проверьте статус в разделе **Actions** на GitHub

### Ручной деплой конкретной версии

1. Перейдите в **Actions** → **Deploy to Cloudflare Pages**
2. Нажмите **Run workflow**
3. Введите версию билда (например, `version-123`)
4. Нажмите **Run workflow**

## Проверка деплоя

После успешного деплоя ваш сайт будет доступен по адресу:
```
https://burky-cz.pages.dev
```

### Просмотр логов деплоя

1. Перейдите в **Actions** на GitHub
2. Выберите последний workflow run
3. Откройте job "Deploy to Cloudflare Pages"
4. Просмотрите логи каждого шага

## Troubleshooting

### ❌ "No builds found"

**Проблема:** GitHub Actions не может найти билды

**Решение:**
- Убедитесь, что вы закоммитили папку `wp-content/uploads/builds/`
- Проверьте, что в билде есть файл `index.html`

### ❌ "API Token invalid"

**Проблема:** Неверный Cloudflare API токен

**Решение:**
- Проверьте, что секрет `CLOUDFLARE_API_TOKEN` добавлен правильно
- Убедитесь, что токен не истек

### ❌ "Project not found"

**Проблема:** Проект не найден на Cloudflare

**Решение:**
- Проверьте `CLOUDFLARE_PROJECT_NAME` (должно быть `burky-cz`)
- Убедитесь, что проект создан на Cloudflare Pages

## Workflow файл

Workflow находится в `.github/workflows/deploy-to-cloudflare.yml`

### Что он делает:

1. **Checkout** - клонирует репозиторий
2. **Determine build** - находит последний билд или использует указанную версию
3. **Deploy** - деплоит на Cloudflare Pages
4. **Summary** - выводит информацию о деплое

### Триггеры:

- Автоматически при push в `main` или `master`
- Вручную через GitHub Actions UI

## Полезные команды

### Проверить токен Cloudflare
```bash
curl "https://api.cloudflare.com/client/v4/user/tokens/verify" \
  -H "Authorization: Bearer 5YA9j_EauFJP772qcvl_Rzsktgk2Bpr3oSiLjQkX"
```

### Посмотреть последний билд
```bash
ls -lt wp-content/uploads/builds/ | head -5
```

### Закоммитить новый билд
```bash
git add wp-content/uploads/builds/
git commit -m "Add build version-$(date +%s)"
git push
```

## Дополнительно

### Кастомные домены

Чтобы использовать свой домен:
1. Перейдите в Cloudflare Pages → burky-cz → Custom domains
2. Добавьте ваш домен
3. Настройте DNS записи

### Переменные окружения

Если нужны переменные окружения для билда:
1. Cloudflare Pages → burky-cz → Settings → Environment variables
2. Добавьте необходимые переменные

---

**Готово!** Теперь каждый push в main автоматически деплоит ваш сайт на Cloudflare Pages 🚀
